import { Section } from "@/components/ui/Section";
import { SectionHeading } from "@/components/ui/SectionHeading";

export function Templates() {
  return (
    <Section id="templates" alt>
      <SectionHeading
        eyebrow="Templates"
        title="Um catálogo de templates à sua escolha"
        lede="Oferecemos templates refinados e funcionais, pensados para diferentes perfis de hospitalidade. Você escolhe o que melhor representa a identidade do seu hotel, e pode trocar quando quiser, sem perder módulos ou dados."
        maxWidth="680px"
      />
    </Section>
  );
}
