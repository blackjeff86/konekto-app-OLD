/// Um slide do carrossel de onboarding — eyebrow (rótulo pequeno acima do
/// título, opcional), título e subtítulo.
class GuestTemplateOnboardingSlide {
  final String? eyebrow;
  final String title;
  final String subtitle;

  const GuestTemplateOnboardingSlide({this.eyebrow, required this.title, required this.subtitle});
}
